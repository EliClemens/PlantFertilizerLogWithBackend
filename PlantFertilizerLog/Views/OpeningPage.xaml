<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PlantFertilizerLog.Views.OpeningPage"
             Title="Welcome"
             BackgroundColor="Black">

    <Grid>
        <!-- Background Image -->
        <Image Source="plant_background.jpg" Aspect="AspectFill" />

        <!-- Overlay Content -->
        <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Padding="30">
            <Label Text="Plant Fertilizer Log"
                   FontSize="48"
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalTextAlignment="Center" />
            <Button Text="Open App"
                    Clicked="OnOpenClicked"
                    BackgroundColor="Green"
                    TextColor="White"
                    FontSize="24"
                    CornerRadius="12"
                    Margin="0,40,0,0" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
